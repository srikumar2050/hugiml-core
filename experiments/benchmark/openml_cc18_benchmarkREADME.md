# OpenML-CC18 benchmark

The maintained downloader, runner, results layout, protocol description, and
commands are documented in [`openml_cc18/README.md`](openml_cc18/README.md).

The benchmark-root entry points forward to the maintained modules in
`openml_cc18/`, so both command forms execute the same implementation.
